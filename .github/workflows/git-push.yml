name: git push into another repo to deploy to vercel

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: checkout source
        uses: actions/checkout@v4

      - name: create output
        run: sh ./build.sh

      - name: push to personal repo
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.YOHAN_DEPLOY_GH_TOKEN }} # 🔑 시크릿 이름
        with:
          source-directory: "output"
          destination-github-username: ghddygks45 # ❗ 너 개인 GitHub 아이디
          destination-repository-name: codeit-Coworkers # ❗ fork한 개인 레포 이름
          user-email: ${{ secrets.YOHAN_GH_EMAIL }} # 📧 시크릿 이름
          commit-message: "chore: sync from team repo"
          target-branch: main
